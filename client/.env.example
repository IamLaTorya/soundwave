# In development, leave this EMPTY. An empty value means requests use
# relative paths like "/api/albums", which the Vite proxy handles.
#
# In production, set this in your hosting dashboard (Vercel/Netlify)
# to the full address of your deployed API, with NO trailing slash:
#   VITE_API_URL=https://soundwave-api.onrender.com

VITE_API_URL=