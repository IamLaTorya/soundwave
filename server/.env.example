# Copy to .env and adjust. Never commit .env.

# Locally this is 5000. In production the hosting platform sets it
# for you — that's why the code reads process.env.PORT.
PORT=5000

# Which website is allowed to read this API's responses.
# Comma-separate multiple origins.
# NO trailing slash, NO path.
CLIENT_URL=http://localhost:5173